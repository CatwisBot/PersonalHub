import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.radenwhisnu.personalhub',
  appName: 'Personal Hub',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
